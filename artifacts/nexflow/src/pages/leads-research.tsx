import IntelligencePage from "@/pages/intelligence";

/**
 * Leads → Research — thin re-export of IntelligencePage with the new
 * top-level label. (The underlying page is unchanged.)
 */
export default function LeadsResearchPage() {
  return <IntelligencePage />;
}
